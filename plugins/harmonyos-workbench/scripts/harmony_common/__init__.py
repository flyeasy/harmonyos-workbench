"""Private shared modules for the HarmonyOS Workbench runtime.

Import the required submodule directly.  Keeping this package initializer empty
avoids loading discovery, target and profile code for commands that only need a
small helper such as evidence serialization.
"""
